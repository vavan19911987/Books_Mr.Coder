НЕ РЕШОННЫЕ ЗАДАЧИ


// Напишите код, который будет проверять число на то, простое оно или нет.
//     Простое число делится только на единицу и на само себя,
//     и не делится на другие числа.

let arr = [2, 3, 5, 7, 11, 1, 4, 6, 12, 13, 42, 43];

for (let i = 0; i <= arr.length -1; i++) {
    // arr[i] /= 2;
    if (arr[i] % 2 >= 1 ) {
        console.log(arr[i]);
    }

}